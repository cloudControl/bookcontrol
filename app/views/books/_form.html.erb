<%= form_for(@book) do |f| %>
  <% if @book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@book.errors.count, "error") %> prohibited this book from being saved:</h2>

      <ul>
      <% @book.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :isbn %><br />
    <%= f.text_field :isbn, :id => 'isbn' %>
    <legend><small>Type the ISBN number and give OpenLibrary a try to fetch the rest.</small></legend>
  </div>
  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title, :id => 'title' %>
  </div>
  <div class="field">
    <%= f.label :author %><br />
    <%= f.text_field :author, :id => 'author' %>
  </div>
  <div class="field">
    <%= f.label :publisher %><br />
    <%= f.text_field :publisher, :id => 'publisher' %>
  </div>
  <div class="field">
    <%= f.label :category %><br />
    <%= f.text_field :category, :id => 'category' %>
  </div>
  <div class="field">
    <%= f.label :img_url %><br />
    <%= f.text_field :img_url, :id => 'img_url' %>
  </div>
  <div class="field">
    <%= f.label :link %><br />
    <%= f.text_field :link, :id => 'link' %>
  </div>
  <div class="field">
    <%= f.label :proposal %><br />
    <%= f.check_box :proposal %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
